# Google Cloud Vision API Configuration
# Copy this file to .env.local and configure your credentials

# Option 1: Service Account Key File (Recommended)
# 1. Download your service account JSON key from Google Cloud Console
# 2. Save it as 'google-vision-key.json' in the project root
# 3. Uncomment the line below
# GOOGLE_APPLICATION_CREDENTIALS=./google-vision-key.json

# Option 2: API Key (Alternative, less secure)
# GOOGLE_VISION_API_KEY=your_api_key_here

# OCR Strategy:
# - "hybrid" (default) = Try Google Vision first, fall back to Tesseract
# - "google-vision" = Use only Google Vision API
# - "tesseract" = Use only Tesseract.js (free, no setup required)
OCR_STRATEGY=hybrid

# Note: If Google Vision credentials are not configured,
# the system will automatically use Tesseract.js as fallback


# Google Analytics
NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Site Configuration
NEXT_PUBLIC_SITE_URL=https://cloud9profile.com
